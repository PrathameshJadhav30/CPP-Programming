#include <iostream>  // Include the standard I/O library for input/output operations
using namespace std;

int main() {
    string name;  // Declare a string variable to store the name
    name = "My name is 3shaank";  // Assign a string value to the variable 'name'

    // Output the length of the string, including whitespaces
    cout << "The length of the string is: " << name.length() << endl;

    return 0;  // Return 0 to indicate successful execution
}
