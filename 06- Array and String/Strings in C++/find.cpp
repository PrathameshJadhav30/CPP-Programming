#include <iostream>  // Include the standard I/O library for input/output operations
using namespace std;

int main() {
    string str1("3sh");  // Declare and initialize the string with "3sh"

    // Find the index of the first occurrence of the character 's' in the string
    cout << "The index of 's' is: " << str1.find('s') << endl; 

    return 0;  // Return 0 to indicate successful execution
}
