#include <stdio.h> // Include the necessary header for standard I/O functions

int main() {
    int a = 25; // Declare and initialize the integer variable 'a' with value 25

    // Print the value of 'a' using printf function
    printf("Value of a: %d\n", a); // The format specifier %d is used for integers

    return 0; // Exit the program successfully
}
