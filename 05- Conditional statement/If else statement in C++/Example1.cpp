#include <iostream>
using namespace std;

int main() {
    int i = 5;  // Initializing the variable `i` with the value 5
    
    // Checking the condition and taking the decision
    if(i > 1) {
        cout << "PrepInsta is one of the best for placement preparation";  // Prints this message if `i > 1`
    }
    else {
        cout << "PrepInsta";  // Prints this message if the condition `i > 1` is false
    }

    return 0;  // Exit the program
}
