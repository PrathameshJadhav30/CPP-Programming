#include <bits/stdc++.h> // Includes all standard libraries (not recommended for production)
using namespace std;

int main() {
    // Declare a variable to store the roll number
    int roll;

    // Prompt the user to enter their roll number
    cout << "Enter your roll number: ";
    cin >> roll;

    // Display the entered roll number
    cout << "\nYour roll number is: " << roll << endl;

    return 0; // Exit the program successfully
}
