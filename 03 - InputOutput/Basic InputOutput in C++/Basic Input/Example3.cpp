#include <iostream> // Only include necessary header for I/O operations
using namespace std;

int main() {
    // Output a message to the standard log stream (clog)
    clog << "An error occurred" << endl;

    return 0; // Exit the program successfully
}
